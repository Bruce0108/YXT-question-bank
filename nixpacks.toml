[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip3 install -r requirements.txt"]

[start]
cmd = "python3 -m gunicorn app:app --workers 1 --threads 8 --bind 0.0.0.0:$PORT --timeout 300 --worker-class gthread"
