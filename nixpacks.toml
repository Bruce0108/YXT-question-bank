[phases.setup]
nixPkgs = ["python313", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m gunicorn app:app --workers 1 --threads 8 --bind 0.0.0.0:$PORT --timeout 300 --worker-class gthread"
