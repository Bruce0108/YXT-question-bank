[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["python3.12 -m pip install -r requirements.txt"]

[start]
cmd = "python3.12 -m gunicorn app:app --workers 1 --threads 8 --bind 0.0.0.0:$PORT --timeout 300 --worker-class gthread"
